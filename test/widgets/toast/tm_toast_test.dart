import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:tm_lib_core/utils/test_utils.dart';
import 'package:tm_lib_core/widgets/toast/tm_toast.dart';

void main() {	
	group('TmToast test', () {
		// testWidgets('TmToast is properly created', (tester) async {
		// 	await tester.pumpWidget(
		// 		Builder(
		// 			builder: (context) {
		// 				TmToast.showError(title: 'Error', context: context);
						
		// 				final text = tester.widget<Text>(find.byType(Text));
		// 				expect(text.data, 'Error');
		// 				return Container();
		// 			},
		// 		),
		// 	);
		// });
	});
}